# Regwrite

Freelance VB6 RegWrite (`RegWrite.exe`) that writes registry values from command-line ROOT/KEY/VALUE/DATA/TYPE args (supports REG_SZ and REG_EXPAND_SZ, plus `$time$`/`$date$`/`$computername$`/`$username$` substitutions). Open `Regwrite.vbp` in the VB6 IDE.

**Source last updated:** 2026-08-27 · **Language:** VB6 · **Target:** VB6 Win32 · **Output:** WinForms exe

## Solution structure

| Project | Language | Type | Purpose |
|---------|----------|------|---------|
| `RegWrite` (`Regwrite.vbp`) | VB6 | WinForms exe | Writes values to the registry |

## How to open

Open the `.vbp` in Visual Basic 6.0 IDE:
- `Regwrite.vbp`

## Requirements

- Visual Basic 6.0 IDE

## Attribution and provenance

Working copy from Dave Robinson's OneDrive Historical Dev folder `VB/Regwrite`.
Company names in `.vbp` files: Freelance.

## License

MIT © 2026 VaderConsulting for Dave Robinson's code. See `LICENSE`.
